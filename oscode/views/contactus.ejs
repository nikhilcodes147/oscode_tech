<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Contact Us→ "Get in Touch – OSCode NMIT"</title>
    <script src="https://cdn.tailwindcss.com"></script>

    <style>
        body {
            background: linear-gradient(135deg, #0f172a, #1e40af);
            color: white;
            font-family: 'Arial', sans-serif;
        }

        .glassmorphism {
            background: rgba(255, 255, 255, 0.1);
            backdrop-filter: blur(10px);
            border-radius: 12px;
            border: 1px solid rgba(255, 255, 255, 0.2);
        }

        .fade-in {
            opacity: 0;
            transform: translateY(20px);
            animation: fadeIn 1s ease-out forwards;
        }

        @keyframes fadeIn {
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }
    </style>
</head>

<body class="flex flex-col items-center justify-center min-h-screen px-4">
    
    <h1 class="text-4xl font-bold text-blue-400 fade-in">Contact Us</h1>

    <div class="flex flex-col md:flex-row w-full max-w-4xl mt-8 gap-8">
        
        <div class="glassmorphism p-6 flex-1 text-center flex flex-col items-center fade-in delay-200">
            <h2 class="text-2xl font-semibold text-blue-300">Get in Touch</h2>
            <p class="mt-2 text-gray-300">Feel free to reach out via email or social media.</p>

            <div class="mt-4 space-y-3">
                <p class="text-lg"><span class="font-semibold text-blue-300">📧 Email:</span> enquiries.oscode@nmit.ac.in</p>
                <div class="flex gap-4 mt-2 text-xl">
                    <a href="https://www.instagram.com/oscode_nmit/" class="hover:text-blue-400 transition">📸 Instagram</a>
                    <a href="#" class="hover:text-blue-400 transition">✖️ Twitter</a>
                    <a href="https://www.linkedin.com/company/oscode-nmit?trk=public_profile_experience-item_profile-section-card_image-click" class="hover:text-blue-400 transition">🔗 LinkedIn</a>
                </div>
            </div>
        </div>

        <div class="glassmorphism p-6 flex-1 fade-in delay-400">
            <h2 class="text-2xl font-semibold text-blue-300 text-center">Send Us a Message</h2>

            <form id="contactForm" class="mt-4 space-y-4">
                <input type="text" id="name" placeholder="Your Name" required
                    class="w-full p-3 rounded-lg bg-transparent border border-gray-300 focus:outline-none focus:border-blue-400 transition">
                
                <input type="email" id="email" placeholder="Your Email" required
                    class="w-full p-3 rounded-lg bg-transparent border border-gray-300 focus:outline-none focus:border-blue-400 transition">
                
                <textarea id="message" rows="4" placeholder="Your Message" required
                    class="w-full p-3 rounded-lg bg-transparent border border-gray-300 focus:outline-none focus:border-blue-400 transition"></textarea>

                <button type="submit"
                    class="w-full p-3 bg-blue-600 hover:bg-blue-500 text-white rounded-lg transition">
                    🚀 Send Message
                </button>
            </form>
        </div>
    </div>

    <p class="mt-6 text-gray-400 fade-in delay-600">Made with ❤️ by OSCode Team</p>

    <script>
        document.getElementById("contactForm").addEventListener("submit", function(event) {
            event.preventDefault();
            let name = document.getElementById("name").value;
            alert(`Thanks ${name}, we will get back to you shortly!`);
            setTimeout(() => {
                window.location.href = "/";
            }, 2000);
        });
    </script>
</body>

</html>